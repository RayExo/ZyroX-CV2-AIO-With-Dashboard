# ── Python dependencies ─────────────────────────────────────────────────────
# Security cleanup (audit H18): removed invalid / typo-squat / stdlib-redundant
# entries that could pull unintended packages from PyPI:
#   - discord           (NOT discord.py — different/abandoned package)
#   - discord.ui        (a module inside discord.py, not a distribution)
#   - discord.ext-context (not a valid PyPI package)
#   - collection        (typo of stdlib `collections`; `collection` is different code)
#   - asyncio, datetime, typing, pathlib  (all stdlib)
#   - duplicate `Requests` (pip is case-insensitive; merged into `requests`)
# For reproducible production installs, pin exact versions with:
#   pip freeze > requirements.lock   &&   pip install -r requirements.lock
Quart
discord.py
jishaku
mcstatus
discord-ext-menus
aiohttp
flask
psutil
requests
pymongo
wavelink>=2.0.0
httpx
tasksio
motor
Augmentor
numpy
aiosqlite
gtts
langdetect
python-dotenv
pyttsx3
PyYAML
openai
duckduckgo-search==6.3.6
colorama
chess
python-dateutil
deep-translator==1.11.4
uvicorn==0.46.0
fastapi
slowapi
pycloudflared
pillow
